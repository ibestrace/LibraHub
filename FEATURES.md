# LibraHub 功能列表文档

> 文档版本: v1.0  
> 最后更新: 2026-02-12  
> 适用版本: LibraHub v1.0.0

---

## 文档说明

本文档详细记录 LibraHub 图书管理系统的所有功能模块，包括：
- 功能描述和业务流程
- 可执行的具体任务
- 涉及的数据字段
- 业务规则和限制
- 后续优化建议

---

## 目录

1. [系统概览](#1-系统概览)
2. [数据概览模块](#2-数据概览模块)
3. [书籍管理模块](#3-书籍管理模块)
4. [会员管理模块](#4-会员管理模块)
5. [借阅归还模块](#5-借阅归还模块)
6. [数据管理模块](#6-数据管理模块)
7. [系统设置模块](#7-系统设置模块)
8. [后续开发计划](#8-后续开发计划)

---

## 1. 系统概览

### 1.1 系统信息

| 项目 | 内容 |
|------|------|
| 系统名称 | LibraHub |
| 副标题 | 智能图书管理系统 |
| 技术架构 | React + TypeScript + Tailwind CSS |
| 数据存储 | 浏览器 LocalStorage |
| 部署方式 | 静态网页，可部署到任何 Web 服务器 |

### 1.2 核心业务流程图

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   书籍录入   │────▶│   会员注册   │────▶│   借阅流程   │
└─────────────┘     └─────────────┘     └──────┬──────┘
                                                │
                       ┌────────────────────────┘
                       ▼
                ┌─────────────┐     ┌─────────────┐
                │   续借操作   │────▶│   归还处理   │
                └─────────────┘     └─────────────┘
```

---

## 2. 数据概览模块

### 2.1 模块说明
提供图书馆运营数据的实时统计和可视化展示，帮助管理员快速了解整体运营状况。

### 2.2 统计卡片

| 卡片名称 | 数据来源 | 更新频率 | 说明 |
|---------|---------|---------|------|
| 总藏书量 | `books.length` | 实时 | 系统中录入的所有书籍数量 |
| 注册会员 | `members.length` | 实时 | 所有注册会员总数 |
| 有效会员 | `members.status === 'active'` | 实时 | 状态为有效的会员数 |
| 当前借出 | `borrowRecords.status === 'borrowed' \|\| 'overdue'` | 实时 | 尚未归还的借阅数量 |
| 逾期未还 | `borrowRecords.status === 'overdue'` | 实时 | 超过应还日期未归还的数量 |

### 2.3 今日动态

| 指标 | 计算规则 | 用途 |
|------|---------|------|
| 今日借阅 | `borrowDate` 为今天的记录数 | 了解每日借出活跃度 |
| 今日归还 | `returnDate` 为今天的记录数 | 了解每日归还情况 |
| 本月新会员 | `createdAt` 在本月的会员数 | 了解会员增长趋势 |

### 2.4 可执行任务

- [ ] 查看实时统计数据
- [ ] 查看当前时间（系统信息）
- [ ] 查看图书馆名称（可配置）
- [ ] 查看系统运行状态

### 2.5 后续优化建议

| 优先级 | 功能 | 描述 |
|--------|------|------|
| P1 | 趋势图表 | 添加近7天/30天的借阅趋势折线图 |
| P2 | 热门书籍排行 | 显示借阅次数最多的前10本书 |
| P3 | 活跃会员排行 | 显示借阅最活跃的会员 |
| P3 | 分类统计饼图 | 各分类书籍占比可视化 |

---

## 3. 书籍管理模块

### 3.1 模块说明
管理系统中的所有书籍信息，支持扫码录入、编辑、删除、搜索等功能。

### 3.2 书籍数据结构

```typescript
interface Book {
  id: string;                    // 系统生成的唯一ID
  barcode: string;               // 条形码（必填，唯一）
  isbn: string;                  // ISBN号
  title: string;                 // 书名（必填）
  subtitle?: string;             // 副标题
  author: string;                // 作者（必填）
  translator?: string;           // 译者
  publisher: string;             // 出版社
  publishDate?: string;          // 出版日期
  edition?: string;              // 版次
  categoryId: string;            // 分类ID
  categoryName?: string;         // 分类名称（冗余存储）
  language?: string;             // 语言（默认中文）
  wordCount?: number;            // 字数
  pageCount?: number;            // 页数
  price?: number;                // 定价
  description?: string;          // 简介
  cover?: string;                // 封面图片URL
  location?: string;             // 馆藏位置
  status: BookStatus;            // 状态（见下方）
  totalStock: number;            // 总库存
  availableStock: number;        // 可借库存
  borrowCount: number;           // 借阅次数（自动统计）
  createdAt: string;             // 创建时间
  updatedAt: string;             // 更新时间
}

type BookStatus = 'available' | 'borrowed' | 'reserved' | 'damaged' | 'lost' | 'under_repair';
```

### 3.3 书籍状态说明

| 状态值 | 显示名称 | 含义 | 是否可借阅 |
|--------|---------|------|-----------|
| available | 可借阅 | 书籍在馆，可以借出 | ✓ |
| borrowed | 已借出 | 所有库存都已借出 | ✗ |
| reserved | 已预约 | 被会员预约 | ✗ |
| damaged | 损坏 | 书籍损坏，暂停借阅 | ✗ |
| lost | 丢失 | 书籍已丢失 | ✗ |
| under_repair | 维修中 | 书籍在维修中 | ✗ |

### 3.4 功能列表

#### 3.4.1 添加书籍

| 项目 | 内容 |
|------|------|
| **功能描述** | 录入新书籍到系统 |
| **入口** | 书籍管理页面 → "添加书籍"按钮 |
| **必填字段** | 条形码、书名、作者 |
| **选填字段** | ISBN、出版社、分类、库存、字数、页数、定价 |
| **业务规则** | 1. 条形码必须唯一  <br>2. 总库存默认为1  <br>3. 可借库存等于总库存 |
| **可执行任务** | - [ ] 手动输入书籍信息  <br>- [ ] 扫码枪扫描条形码自动录入  <br>- [ ] 选择书籍分类  <br>- [ ] 设置初始库存 |

#### 3.4.2 编辑书籍

| 项目 | 内容 |
|------|------|
| **功能描述** | 修改已有书籍的信息 |
| **入口** | 书籍列表 → 行尾编辑按钮 |
| **可修改字段** | 除ID、创建时间、借阅次数外的所有字段 |
| **业务规则** | 1. 条形码修改时需检查唯一性  <br>2. 可借库存不能超过总库存  <br>3. 修改后自动更新 `updatedAt` |
| **可执行任务** | - [ ] 修改书籍基本信息  <br>- [ ] 调整库存数量  <br>- [ ] 更改书籍状态  <br>- [ ] 更换分类 |

#### 3.4.3 删除书籍

| 项目 | 内容 |
|------|------|
| **功能描述** | 从系统中删除书籍 |
| **入口** | 书籍列表 → 行尾删除按钮 |
| **业务规则** | 1. 有未归还借阅记录的书籍不能删除  <br>2. 删除前需二次确认  <br>3. 删除操作记录到日志 |
| **可执行任务** | - [ ] 删除单本书籍  <br>- [ ] 批量删除（未来支持） |

#### 3.4.4 搜索和筛选

| 项目 | 内容 |
|------|------|
| **功能描述** | 快速查找书籍 |
| **搜索方式** | 关键词搜索、分类筛选、状态筛选 |
| **可搜索字段** | 书名、作者、条形码、ISBN |
| **可执行任务** | - [ ] 关键词搜索  <br>- [ ] 按分类筛选  <br>- [ ] 按状态筛选  <br>- [ ] 清除筛选条件  <br>- [ ] 分页浏览 |

#### 3.4.5 扫码录入

| 项目 | 内容 |
|------|------|
| **功能描述** | 使用扫码枪快速录入书籍 |
| **触发方式** | 点击"扫码录入"按钮 |
| **工作流程** | 1. 进入扫码模式  <br>2. 扫码枪扫描条形码  <br>3. 如果是新书，打开添加弹窗并预填充条形码  <br>4. 如果是已有书，打开编辑弹窗 |
| **可执行任务** | - [ ] 开启/关闭扫码模式  <br>- [ ] 扫描条形码识别书籍  <br>- [ ] 自动填充条形码到表单 |

#### 3.4.6 数据导出

| 项目 | 内容 |
|------|------|
| **功能描述** | 将书籍数据导出为JSON文件 |
| **入口** | 书籍管理页面 → "导出"按钮 |
| **可执行任务** | - [ ] 导出全部书籍数据  <br>- [ ] 导出的文件包含所有字段 |

### 3.5 后续优化建议

| 优先级 | 功能 | 描述 |
|--------|------|------|
| P1 | ISBN自动获取 | 输入ISBN后自动从豆瓣/Google Books API获取书籍信息 |
| P1 | 批量导入 | 支持Excel/CSV批量导入书籍 |
| P2 | 封面上传 | 支持上传本地图片作为书籍封面 |
| P2 | 书籍推荐 | 根据借阅历史推荐相关书籍 |
| P3 | 书籍评价 | 会员可以对书籍进行评分和评论 |
| P3 | 书架位置管理 | 更详细的馆藏位置管理（楼层、书架、层号） |

---

## 4. 会员管理模块

### 4.1 模块说明
管理图书馆会员信息，支持会员注册、类型设置、状态管理等功能。

### 4.2 会员数据结构

```typescript
interface Member {
  id: string;                    // 系统生成的唯一ID
  cardNumber: string;            // 会员卡号（必填，唯一，用于扫码）
  name: string;                  // 姓名（必填）
  gender?: 'male' | 'female' | 'other';  // 性别
  phone: string;                 // 电话（必填）
  email?: string;                // 邮箱
  address?: string;              // 地址
  idCard?: string;               // 身份证号
  birthDate?: string;            // 出生日期
  memberType: MemberType;        // 会员类型（对象）
  status: MemberStatus;          // 会员状态
  registerDate: string;          // 注册日期
  expireDate: string;            // 到期日期
  maxBorrowCount: number;        // 最大借阅数量（从会员类型继承）
  currentBorrowCount: number;    // 当前借阅数量（自动计算）
  deposit?: number;              // 押金金额
  notes?: string;                // 备注
  createdAt: string;             // 创建时间
  updatedAt: string;             // 更新时间
}

type MemberStatus = 'active' | 'expired' | 'suspended' | 'cancelled';
```

### 4.3 会员类型数据结构

```typescript
interface MemberType {
  id: string;                    // 类型ID
  name: string;                  // 类型名称（如：普通会员、VIP会员）
  durationMonths: number;        // 有效期（月）
  maxBorrowCount: number;        // 最大借阅数量
  maxBorrowDays: number;         // 最大借阅天数
  renewTimes: number;            // 可续借次数
  renewDays: number;             // 每次续借天数
  depositAmount: number;         // 押金金额
  fee: number;                   // 会费
  description?: string;          // 描述
  createdAt: string;
  updatedAt: string;
}
```

### 4.4 默认会员类型

| 类型名称 | 有效期 | 最大借阅 | 借阅天数 | 续借次数 | 押金 | 会费 |
|---------|--------|---------|---------|---------|------|------|
| 普通会员 | 12月 | 5本 | 30天 | 2次 | 100元 | 50元 |
| VIP会员 | 12月 | 10本 | 60天 | 3次 | 200元 | 200元 |

### 4.5 会员状态说明

| 状态值 | 显示名称 | 含义 | 是否可借阅 |
|--------|---------|------|-----------|
| active | 有效 | 会员资格有效 | ✓ |
| expired | 已过期 | 超过到期日期 | ✗ |
| suspended | 已暂停 | 被管理员暂停 | ✗ |
| cancelled | 已注销 | 已注销会员资格 | ✗ |

### 4.6 功能列表

#### 4.6.1 添加会员

| 项目 | 内容 |
|------|------|
| **功能描述** | 注册新会员 |
| **必填字段** | 会员卡号、姓名、电话 |
| **选填字段** | 性别、邮箱、地址、身份证号、出生日期、会员类型、押金、备注 |
| **业务规则** | 1. 会员卡号必须唯一  <br>2. 默认会员类型为"普通会员"  <br>3. 到期日期根据会员类型自动计算  <br>4. 最大借阅数量继承自会员类型 |
| **可执行任务** | - [ ] 手动输入会员信息  <br>- [ ] 扫码枪扫描会员卡号  <br>- [ ] 选择会员类型  <br>- [ ] 设置注册和到期日期 |

#### 4.6.2 编辑会员

| 项目 | 内容 |
|------|------|
| **功能描述** | 修改会员信息 |
| **特殊逻辑** | 更换会员类型时，自动更新 `maxBorrowCount` 和 `expireDate` |
| **业务规则** | 1. 会员卡号修改需检查唯一性  <br>2. 当前借阅数量不能超过新的最大借阅限制  <br>3. 状态改为非 active 时，不能继续借书 |
| **可执行任务** | - [ ] 修改会员基本信息  <br>- [ ] 更换会员类型  <br>- [ ] 延长/缩短有效期  <br>- [ ] 更改会员状态 |

#### 4.6.3 删除会员

| 项目 | 内容 |
|------|------|
| **功能描述** | 删除会员信息 |
| **业务规则** | 1. 有未归还借阅记录的会员不能删除  <br>2. 删除前需二次确认 |
| **可执行任务** | - [ ] 删除单个会员  <br>- [ ] 批量删除（未来支持） |

#### 4.6.4 查看会员详情

| 项目 | 内容 |
|------|------|
| **功能描述** | 查看会员详细信息 |
| **展示内容** | 基本信息、借阅情况进度条、会员类型信息 |
| **可执行任务** | - [ ] 查看会员详细信息  <br>- [ ] 查看借阅进度  <br>- [ ] 快速跳转到编辑页面 |

#### 4.6.5 搜索和筛选

| 项目 | 内容 |
|------|------|
| **搜索方式** | 关键词搜索（姓名、卡号、电话）、状态筛选 |
| **可执行任务** | - [ ] 关键词搜索  <br>- [ ] 按状态筛选  <br>- [ ] 清除筛选  <br>- [ ] 分页浏览 |

#### 4.6.6 扫码识别

| 项目 | 内容 |
|------|------|
| **功能描述** | 扫描会员卡号快速查找会员 |
| **工作流程** | 1. 进入扫码模式  <br>2. 扫描会员卡号  <br>3. 如果是已注册会员，显示详情弹窗  <br>4. 如果是新卡号，打开添加会员弹窗 |

#### 4.6.7 会员类型管理

| 项目 | 内容 |
|------|------|
| **功能描述** | 管理系统中的会员类型 |
| **当前位置** | 数据嵌入在代码中（`MemberTypeService.getAll()`） |
| **业务规则** | 1. 有会员使用的类型不能删除  <br>2. 修改类型参数不影响已存在会员（除非主动更新） |
| **可执行任务** | - [ ] 添加新会员类型  <br>- [ ] 修改会员类型参数  <br>- [ ] 删除未使用的会员类型 |

### 4.7 后续优化建议

| 优先级 | 功能 | 描述 |
|--------|------|------|
| P1 | 会员类型管理界面 | 在系统设置中添加会员类型管理页面 |
| P2 | 会员续费功能 | 支持会员到期后续费延长有效期 |
| P2 | 会员借阅历史 | 查看会员的所有借阅记录 |
| P3 | 会员照片 | 支持上传会员头像/照片 |
| P3 | 会员等级自动升级 | 根据借阅活跃度自动升级会员等级 |

---

## 5. 借阅归还模块

### 5.1 模块说明
核心业务模块，处理书籍的借出、归还、续借等操作。

### 5.2 借阅记录数据结构

```typescript
interface BorrowRecord {
  id: string;                    // 记录ID
  bookId: string;                // 书籍ID
  bookBarcode: string;           // 书籍条形码（冗余）
  bookTitle: string;             // 书名（冗余）
  bookAuthor: string;            // 作者（冗余）
  memberId: string;              // 会员ID
  memberCardNumber: string;      // 会员卡号（冗余）
  memberName: string;            // 会员姓名（冗余）
  borrowDate: string;            // 借阅日期
  dueDate: string;               // 应还日期
  returnDate?: string;           // 实际归还日期
  status: BorrowStatus;          // 借阅状态
  renewCount: number;            // 续借次数
  fineAmount: number;            // 罚款金额
  fineReason?: string;           // 罚款原因
  notes?: string;                // 备注
  operator: string;              // 操作员
  createdAt: string;
  updatedAt: string;
}

type BorrowStatus = 'borrowed' | 'returned' | 'overdue' | 'renewed';
```

### 5.3 借阅状态说明

| 状态值 | 显示名称 | 含义 |
|--------|---------|------|
| borrowed | 借阅中 | 正常借阅，未到还书日期 |
| renewed | 已续借 | 已经办理过续借 |
| overdue | 已逾期 | 超过应还日期未归还 |
| returned | 已归还 | 书籍已归还 |

### 5.4 功能列表

#### 5.4.1 借书流程

| 项目 | 内容 |
|------|------|
| **功能描述** | 处理会员借书业务 |
| **入口** | 借阅归还页面 → "借书"视图 |
| **流程步骤** | 1. 扫描会员卡号 → 验证会员  <br>2. 扫描书籍条形码 → 验证书籍  <br>3. 确认借阅信息  <br>4. 完成借阅 |
| **业务规则** | 1. 会员状态必须有效  <br>2. 会员有逾期未还书籍时不能借书（可配置）  <br>3. 会员当前借阅数量不能超过上限  <br>4. 书籍状态必须可借，库存必须大于0  <br>5. 应还日期 = 今天 + 会员类型的 `maxBorrowDays` |
| **可执行任务** | - [ ] 扫描会员卡号  <br>- [ ] 扫描书籍条形码  <br>- [ ] 查看会员可借数量  <br>- [ ] 确认并完成借阅  <br>- [ ] 取消借阅操作 |

#### 5.4.2 还书流程

| 项目 | 内容 |
|------|------|
| **功能描述** | 处理会员还书业务 |
| **入口** | 借阅归还页面 → "还书/续借"视图 → 选择记录 |
| **流程步骤** | 1. 选择要归还的借阅记录  <br>2. 系统自动计算是否逾期  <br>3. 如逾期，计算罚款金额  <br>4. 确认并处理归还 |
| **业务规则** | 1. 只能归还状态为 borrowed 或 overdue 的记录  <br>2. 逾期罚款 = 逾期天数 × 每天罚款金额（系统设置）  <br>3. 归还后书籍可借库存 +1  <br>4. 归还后会员当前借阅数量 -1  <br>5. 罚款金额可手动调整 |
| **可执行任务** | - [ ] 搜索借阅记录  <br>- [ ] 选择要归还的记录  <br>- [ ] 查看逾期罚款  <br>- [ ] 调整罚款金额  <br>- [ ] 添加备注  <br>- [ ] 确认归还 |

#### 5.4.3 续借功能

| 项目 | 内容 |
|------|------|
| **功能描述** | 延长书籍借阅期限 |
| **入口** | 借阅归还页面 → "还书/续借"视图 → 选择记录 |
| **业务规则** | 1. 只能续借状态为 borrowed 的记录（不能续借已逾期的）  <br>2. 续借次数不能超过会员类型的 `renewTimes`  <br>3. 新的应还日期 = 原应还日期 + 会员类型的 `renewDays`  <br>4. 续借后状态变为 renewed |
| **可执行任务** | - [ ] 查看可续借次数  <br>- [ ] 确认续借  <br>- [ ] 查看新的应还日期 |

#### 5.4.4 当前借阅列表

| 项目 | 内容 |
|------|------|
| **功能描述** | 查看所有未归还的借阅记录 |
| **入口** | 借阅归还页面 → "当前借阅"视图 |
| **显示字段** | 书籍信息、会员信息、借阅日期、应还日期、状态、操作按钮 |
| **可执行任务** | - [ ] 搜索借阅记录  <br>- [ ] 按书名/会员/条形码筛选  <br>- [ ] 查看逾期状态  <br>- [ ] 快速归还/续借 |

#### 5.4.5 逾期提醒

| 项目 | 内容 |
|------|------|
| **功能描述** | 查看所有逾期未还的借阅记录 |
| **入口** | 借阅归还页面 → "逾期提醒"视图 |
| **特殊功能** | 导航按钮显示逾期数量徽章 |
| **显示内容** | 逾期天数、逾期罚款预估 |
| **可执行任务** | - [ ] 查看所有逾期记录  <br>- [ ] 查看逾期天数  <br>- [ ] 处理归还 |

#### 5.4.6 逾期状态自动更新

| 项目 | 内容 |
|------|------|
| **触发时机** | 1. 获取逾期列表时  <br>2. 获取统计数据时  <br>3. 每次借阅操作时 |
| **更新逻辑** | 检查所有 `status === 'borrowed'` 且 `dueDate < now()` 的记录，自动将状态更新为 'overdue' |

### 5.5 后续优化建议

| 优先级 | 功能 | 描述 |
|--------|------|------|
| P1 | 预约功能 | 会员可以预约已借出的书籍 |
| P2 | 逾期通知 | 系统自动发送逾期提醒（邮件/短信） |
| P2 | 自助借还 | 会员自助操作借还（无需管理员） |
| P3 | 借阅历史查询 | 查看已归还的历史记录 |
| P3 | 借阅排行榜 | 热门书籍借阅排行 |

---

## 6. 数据管理模块

### 6.1 模块说明
提供系统数据的备份、恢复、清理功能，保障数据安全。

### 6.2 数据概览

| 数据类型 | 统计项 | 说明 |
|---------|--------|------|
| 书籍数据 | 总藏书量 | 系统中录入的书籍数量 |
| 会员数据 | 总会员数 | 注册的会员数量 |
| 借阅记录 | 总记录数 | 所有借阅操作记录 |
| 数据大小 | 存储占用 | 所有数据占用的存储空间 |

### 6.3 功能列表

#### 6.3.1 数据备份（导出）

| 项目 | 内容 |
|------|------|
| **功能描述** | 将所有数据导出为JSON文件 |
| **导出内容** | 书籍、会员、会员类型、借阅记录、分类、设置、日志 |
| **文件格式** | JSON，带缩进便于阅读 |
| **文件名格式** | `library_backup_YYYYMMDD_HHmmss.json` |
| **可执行任务** | - [ ] 执行数据备份  <br>- [ ] 下载备份文件  <br>- [ ] 查看备份文件大小 |

#### 6.3.2 数据恢复（导入）

| 项目 | 内容 |
|------|------|
| **功能描述** | 从备份文件恢复数据 |
| **工作流程** | 1. 选择备份文件  <br>2. 预览备份信息（版本、时间、数据量）  <br>3. 确认恢复  <br>4. 覆盖现有数据 |
| **业务规则** | 1. 只接受有效的备份文件格式  <br>2. 恢复前预览数据量  <br>3. 恢复会覆盖所有现有数据  <br>4. 恢复后自动刷新页面 |
| **可执行任务** | - [ ] 选择备份文件  <br>- [ ] 预览备份信息  <br>- [ ] 确认恢复数据  <br>- [ ] 取消恢复操作 |

#### 6.3.3 数据清理

| 项目 | 内容 |
|------|------|
| **功能描述** | 清空所有系统数据 |
| **警告级别** | 高危操作，需要二次确认 |
| **清理内容** | 所有 localStorage 数据 |
| **业务规则** | 1. 需要明确的二次确认  <br>2. 清理后页面自动刷新  <br>3. 无法撤销，务必先备份 |
| **可执行任务** | - [ ] 查看当前数据量  <br>- [ ] 执行数据清空  <br>- [ ] 取消清空操作 |

### 6.4 后续优化建议

| 优先级 | 功能 | 描述 |
|--------|------|------|
| P1 | 自动备份 | 定期自动备份到本地/云端 |
| P2 | 增量备份 | 只备份修改过的数据 |
| P2 | 数据同步 | 多设备间数据同步 |
| P3 | 备份加密 | 备份文件加密保护 |
| P3 | 操作日志导出 | 导出操作日志用于审计 |

---

## 7. 系统设置模块

### 7.1 模块说明
配置图书馆基本信息和系统运行参数。

### 7.2 设置数据结构

```typescript
interface SystemSettings {
  libraryName: string;           // 图书馆名称
  libraryAddress?: string;       // 地址
  libraryPhone?: string;         // 联系电话
  libraryEmail?: string;         // 电子邮箱
  maxBorrowDays: number;         // 默认借阅天数
  maxRenewTimes: number;         // 默认续借次数
  renewDays: number;             // 默认续借天数
  overdueFinePerDay: number;     // 逾期罚款（元/天）
  allowOverdueBorrow: boolean;   // 允许逾期借阅
  backupPath?: string;           // 备份路径（预留）
  lastBackupAt?: string;         // 上次备份时间（预留）
}
```

### 7.3 功能列表

#### 7.3.1 图书馆信息设置

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| 图书馆名称 | 文本 | LibraHub 图书馆 | 显示在数据概览页面 |
| 联系电话 | 文本 | 空 | 图书馆联系方式 |
| 电子邮箱 | 文本 | 空 | 图书馆联系邮箱 |
| 地址 | 文本 | 空 | 图书馆地址 |

#### 7.3.2 借阅规则设置

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| 默认借阅天数 | 数字 | 30 | 新会员默认借阅天数 |
| 默认续借次数 | 数字 | 2 | 新会员默认可续借次数 |
| 续借天数 | 数字 | 15 | 每次续借延长天数 |

**注意**: 这些默认设置只影响新创建的会员类型，已有会员类型需要单独修改。

#### 7.3.3 逾期罚款设置

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| 逾期罚款 | 数字 | 1.0 | 每逾期一天罚款金额（元） |
| 允许逾期借阅 | 开关 | false | 会员有逾期书籍时是否允许继续借书 |

#### 7.3.4 系统信息显示

| 信息项 | 内容 |
|--------|------|
| 系统版本 | v1.0.0 |
| 数据存储 | 浏览器本地存储 |
| 技术栈 | React + TypeScript |
| 最后更新 | 2024-02 |

### 7.4 可执行任务

- [ ] 修改图书馆基本信息
- [ ] 调整借阅规则参数
- [ ] 设置逾期罚款金额
- [ ] 开启/关闭逾期借阅限制
- [ ] 保存设置
- [ ] 重置设置到上次保存状态

### 7.5 后续优化建议

| 优先级 | 功能 | 描述 |
|--------|------|------|
| P1 | 会员类型管理 | 在设置页面管理会员类型 |
| P2 | 分类管理 | 在设置页面管理书籍分类 |
| P2 | 操作员管理 | 添加操作员账号和密码 |
| P3 | 系统主题设置 | 支持切换主题色、深色模式 |
| P3 | 语言切换 | 支持多语言（中文/英文） |
| P3 | 打印设置 | 配置小票打印模板 |

---

## 8. 后续开发计划

### 8.1 近期计划（v1.1.0）

| 优先级 | 功能模块 | 具体任务 | 预估工时 |
|--------|---------|---------|---------|
| P0 | 数据同步 | 实现逾期状态自动检测定时器 | 4h |
| P1 | 书籍管理 | ISBN自动获取书籍信息（豆瓣API） | 8h |
| P1 | 会员管理 | 会员类型管理界面 | 6h |
| P1 | 系统设置 | 书籍分类管理界面 | 4h |
| P2 | 数据概览 | 添加趋势图表（近7天借阅趋势） | 6h |
| P2 | 借阅管理 | 预约功能实现 | 10h |

### 8.2 中期计划（v1.2.0）

| 优先级 | 功能模块 | 具体任务 | 预估工时 |
|--------|---------|---------|---------|
| P1 | 会员系统 | 会员续费功能 | 8h |
| P2 | 数据管理 | 自动定期备份 | 6h |
| P2 | 报表系统 | 新增更多统计报表 | 10h |
| P2 | 系统设置 | 操作员账号管理 | 8h |
| P3 | UI优化 | 深色模式支持 | 6h |

### 8.3 远期计划（v2.0.0）

| 优先级 | 功能模块 | 具体任务 | 预估工时 |
|--------|---------|---------|---------|
| P1 | 架构升级 | 后端API + 数据库存储 | 40h |
| P2 | 移动端 | 开发配套移动App | 60h |
| P2 | 自助服务 | 会员自助借还终端 | 30h |
| P3 | 智能推荐 | 基于借阅历史的推荐系统 | 20h |

---

## 附录

### A. 数据存储键名

```typescript
const STORAGE_KEYS = {
  BOOKS: 'library_books',
  MEMBERS: 'library_members',
  MEMBER_TYPES: 'library_member_types',
  BORROW_RECORDS: 'library_borrow_records',
  RESERVATIONS: 'library_reservations',      // 预留
  CATEGORIES: 'library_categories',
  LOGS: 'library_logs',
  SETTINGS: 'library_settings',
  OPERATORS: 'library_operators'             // 预留
};
```

### B. 操作日志类型

```typescript
type LogType = 'book' | 'member' | 'borrow' | 'return' | 'renew' | 'system';
```

### C. 浏览器兼容性

| 浏览器 | 最低版本 | 说明 |
|--------|---------|------|
| Chrome | 80+ | 推荐 |
| Firefox | 75+ | 支持 |
| Safari | 13+ | 支持 |
| Edge | 80+ | 支持 |

### D. LocalStorage 限制

| 浏览器 | 存储限制 | 说明 |
|--------|---------|------|
| Chrome | ~5-10MB | 超过会抛出 QuotaExceededError |
| Firefox | ~5-10MB | 同上 |
| Safari | ~5MB | 同上 |

**建议**: 当数据量较大时，应及时备份并清理旧数据。

---

## 更新记录

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| v1.0 | 2026-02-12 | 初始版本，记录 v1.0.0 功能 |

---

*本文档由 LibraHub 开发团队维护，如有疑问请联系技术支持。*
